# Readme

A simple shell written in Rust.

feature list:
  * Bulitin tools like echo, type, pwd, cd...
  * Quoting with double quotes `"` and single quotes `'`.
  * Backslash Escaping.
  * Execute the executable file on the host.